//#include <reg51.h>  // Include the 8051 microcontroller header file

//sbit LED = P1^0;   // Define LED connected to Port 1, Pin 0

//void delay_ms(unsigned int ms) {
//    unsigned int i, j;
//    for(i = 0; i < ms; i++)
//        for(j = 0; j < 1275; j++);  // Approximate delay for 1ms
//}

//void main() {
//    while(1) {
//        LED = 1;   // Turn ON LED
//        delay_ms(500);  // Delay for 500ms
//        LED = 0;   // Turn OFF LED
//        delay_ms(500);  // Delay for 500ms
//    }
//}
